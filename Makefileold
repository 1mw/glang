SOURCEDIR := src/
CLASSDIR := bin/

JAVA_SOURCES := $(shell find $(SOURCEDIR) -name '*.java')
JAVA_CLASSES := $(JAVA_CLASSES:$(SOURCEDIR)=$(CLASSDIR))
JAVA_CLASSES := $(JAVA_CLASSES:.java:.class)


all: JAVA_CLASSES

$(CLASSDIR)/%.class: $(SOURCEDIR)/%.java:
	javac -cp $(CLASSDIR)